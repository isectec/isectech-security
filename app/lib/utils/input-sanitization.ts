export function sanitizeInput<T = any>(value: T): T {
  // Minimal placeholder to satisfy imports; expand with real sanitization as needed
  return value;
}

export const sanitizeString = (str: string): string => str;

