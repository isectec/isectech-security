FROM python:3.9-slim

WORKDIR /app

# Copy the demo server
COPY simple-demo.py .

# Install Flask
RUN pip install flask flask-cors

# Expose port
EXPOSE 8080

# Run the application
CMD ["python", "simple-demo.py"]